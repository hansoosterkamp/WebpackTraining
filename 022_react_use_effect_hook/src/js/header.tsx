import * as React from "react";
import * as ReactDOM from "react-dom";

// Header component, note React components shoudl start with a capital casing
export const Header = () => {

    return (<header>
        <h1>Eve's kitchen</h1>
    </header>)
}